const video=document.querySelector('#video'), canvas=document.querySelector('#canvas'), status=document.querySelector('#status'), records=document.querySelector('#records');
async function loadRecords(){const data=await (await fetch('/api/attendance')).json();records.innerHTML=data.map(x=>`<tr><td>${x.enrollment_id}</td><td>${x.name}</td><td>${x.attendance_date}</td><td>${x.attendance_time}</td></tr>`).join('')||'<tr><td colspan="4">No attendance yet</td></tr>'}
async function recognize(){if(video.readyState<2)return;canvas.width=video.videoWidth;canvas.height=video.videoHeight;canvas.getContext('2d').drawImage(video,0,0);const r=await fetch('/api/recognize',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({image:canvas.toDataURL('image/jpeg',.85)})});const d=await r.json();if(d.found){const s=d.student;status.textContent=d.student.already_marked?`${s.name} (${s.enrollment_id}) was already marked at ${s.time}`:`Marked: ${s.name} | ${s.enrollment_id} | ${s.date} ${s.time}`;loadRecords()}else status.textContent=d.message||'Trying to recognize face…'}
navigator.mediaDevices.getUserMedia({video:true}).then(s=>{video.srcObject=s;status.textContent='Look at the camera to mark attendance.';setInterval(recognize,3000)}).catch(()=>status.textContent='Camera permission is required.');loadRecords();
